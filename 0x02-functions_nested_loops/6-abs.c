#include "main.h"

/**
 * _abs - return absolute value of a number
 * @n : number to check
 * Return: return absolute value of a number
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);

}
